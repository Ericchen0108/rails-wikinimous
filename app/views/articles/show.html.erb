<h1 ><%= link_to 'Wikinimous', articles_path%></h1>

<h1><%= @article.title %> <%= link_to '✎', edit_article_path%> 
<%= link_to '🗑',
    article_path(@article), 
    method: :delete,
    data: { confirm: "Are you sure?" }%></h1> 
<h3><%= @article.content %></h3>
